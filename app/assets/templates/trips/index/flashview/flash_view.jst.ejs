<h4><%=trip.get("name") || "No trip to display!"%></h4>

<% if (trip.get("name")) { %>
  Location: <h5><%=trip.get("city")%>, <%=trip.get("state")%></h5><br>
  Duration: <h5><%=trip.get("start_date_string")%> to <%=trip.get("end_date_string")%></h5>
  Owned By: <h5><%=trip.get("owner_name")%></h5>
  <div id="pending-requests"></div>
  <h4>You are meeting <%=trip.meetings%> people:</h4>
  <div id="user-meeters"></div>
  <div id="search-users" class="meeter-item">
    <a href="/#search/<%=trip.get("id")%>"><h3>+</h3></a>
  </div>


<% } else { %>
  <p>It seems like you have no trips. Create one on the left to get started!</p>
<% } %>
